# Gorgona Konfiguration - Format: KEY = VALUE, Kommentare mit '#'
# Die zu durchsuchende SQLite-Datenbank (Pflicht):
# db_path = data/test.db
db_path = /Users/olf/Library/Application Support/TablePro/Samples/Chinook.sqlite

# TCP-Port und Anzahl Worker-Threads (optional, Defaults 8080 und 4;
# Kommandozeilen-Argumente ./gorgona <port> <workers> uebersteuern diese):
port = 8080
workers = 4
